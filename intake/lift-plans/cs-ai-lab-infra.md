# CS AI Lab infrastructure lift plan

No source files are approved for staging or promotion. If provenance becomes
confirmed, assess `ollama_local` first as a clean-room implementation: include
only new portable source, fake HTTP fixtures, descriptor, guide, knowledge
record, changelog, and conformance tests. Exclude all host configuration,
credentials, logs, deployment files, and arbitrary command behaviour. Rollback
is removal of the unpublished candidate before admission.
