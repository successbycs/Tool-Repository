# TR-M04 AI Engineer review

Passed: catalogue construction reads descriptor JSON and release-index JSON
only. It never imports adapter modules. Tests cover deterministic generation,
malformed release metadata, untrusted publishers, and checksum mismatch.
