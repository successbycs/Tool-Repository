# TR-M05 AI Engineer review

Passed: the three adapters have static manifests and explicit runtime
conformance tests. Lab and transcription diagnostics require fake or
consumer-supplied fixed probes, so default tests perform no remote activity.
All declared operations are read-only, and invalid targets and malformed probe
results are normalized safely.
