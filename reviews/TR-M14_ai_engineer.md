# TR-M14 AI Engineer review

Passed: the policy makes tool-capability promotion explicit across adapters,
local models, prompts, and harvested assets. Source records and verification
are now first-class milestone inputs, preventing an unsupported catalogue claim
from being confused with an evaluated or installed capability.

Advice: retain the separate role/modality/limitation evidence for each asset
type; the shared ingestion rule should not flatten the distinct evaluation
standards for models, prompts, and executable adapters.
